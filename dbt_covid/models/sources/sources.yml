version: 2

sources:
  - name: bronze
    database: "covid-bigquery-494113"
    schema: covid_raw

    tables:
      - name: global_epidemiology
        identifier: global_epidemiology
        description: "Daily COVID-19 metrics per country from OWID. 525k rows."

      - name: us_state_tracking
        identifier: us_state_tracking
        description: "Daily US state-level tracking from COVID Tracking Project. 20k rows."

      - name: country_summary
        identifier: country_summary
        description: "Per-country aggregate generated via SQL GROUP BY. 239 rows."
