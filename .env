# Copy to `.env` and fill in your real values. Never commit `.env`.
# `.env` is auto-loaded by python-dotenv and can be sourced in shell with
# `set -a; source .env; set +a`.

# ── Assignment 1: PostgreSQL (local) ──────────────────────────────────────
PG_HOST=localhost
PG_PORT=5432
PG_DB=covid_db
PG_USER=postgres
PG_PASSWORD=change-me

# ── Assignment 2: Databricks ──────────────────────────────────────────────
# From Databricks → SQL Warehouses → Serverless Starter → Connection details
DBT_DATABRICKS_HOST=dbc-56c1435d-2960.cloud.databricks.com
DBT_DATABRICKS_HTTP_PATH=/sql/1.0/warehouses/5027cdd44c860a1a

# From User Settings → Developer → Access tokens → Generate new token
# Scope: BI Tools, API scope: sql
DBT_DATABRICKS_TOKEN=dapif93a1edd9172c35fb208f59fc29cd11f